#include <iostream>
#include <sstream>
#include <algorithm>
using namespace std;

int main()
{
    int n=0;
    while(cin>>n)
    {
        // string str;
        // ostringstream os;
        // os<<n;
        // str = os.str();
        // reverse(str.begin(),str.end());
        cout<<n<<endl;
    }
    
    
    return 0;
}